# learning

#for error
root@ubuntu-VirtualBox:~/do-k8s# kubectl get nodes
The connection to the server localhost:8080 was refused - did you specify the right host or port?

export KUBECONFIG=$HOME/admin.conf
